{
  "name": "argus",
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"No JS tests configured yet\"",
    "lint": "echo \"No JS lint configured yet\"",
    "generate:api": "bash scripts/generate-api-contracts.sh"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp",
      "unrs-resolver"
    ]
  }
}
