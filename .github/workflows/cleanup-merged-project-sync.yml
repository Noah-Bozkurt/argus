name: Cleanup merged Payload sync branch

on:
  push:
    branches:
      - agent/payload-cms-v1

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          if git ls-remote --exit-code --heads origin refs/heads/agent/payload-project-sync-v1 >/dev/null 2>&1; then
            git push origin --delete agent/payload-project-sync-v1
          fi
